# Internal contribution notes

Keep protocol changes, schemas, vectors, implementation, tests, and docs in
one change. Use `apply_patch` or normal reviewable edits, preserve unrelated
work, and do not rewrite history. Run `npm run check` for a local full gate.

No public publication, deployment, signing-account setup, domain purchase,
external contact, or license selection is authorized by this repository.
